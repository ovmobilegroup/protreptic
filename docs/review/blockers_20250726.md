# 阻断清单

**生成日期**: 2025-07-26  
**关联 QA 报告**: `docs/review/qa_report_20250726.md`  
**门禁规则**: 任一 P0 项存在 → 直接退回对应船员；P1 项 > 3 → 暂停发布，集中修复

---

## P0 级阻断项（必须修复，零容忍）

| 编号 | 类别 | 问题描述 | 影响面 | 复现步骤 | 责任人 | 预计修复时间 | 验收标准 |
|------|------|----------|--------|----------|--------|--------------|----------|
| **P0-1** | 数据层 | **EN 文件 259 字段中文残留** — reason/steps/case/description/expected 未翻译 | 双语查询功能失效，英文模式返回中文 | `python3 tools/thinking_mode_selector.py -c H-LB-01 --lang en` → 返回中文 | **elcano** (落地层) | 3-5 天 | 所有 289 场景 EN 字段 0 中文字符，双语查询全英输出 |
| **P0-2** | 数据层 | **141/269 历史人物缺失 era 字段** — 时代筛选、统计、展示全挂 | Web UI 时代筛选空白，API 无 era 字段 | 打开 web/index.html → 时代下拉框选"秦汉" → 结果为空 | **elcano** | 2-3 天 | 269 个 H-* 场景 100% 有 era 字段（秦汉/三国两晋/隋唐/宋元/明清/民国/现代等） |
| **P0-3** | 数据层 | **8 组历史人物重复** — 16 个代码映射 8 实体，code_map 值冲突 | 选择器去重逻辑失效，统计失真 | `python3 -c "import json; cm=json.load(open('tools/code_maps.json')); from collections import Counter; v=list(cm['CODE_MAP'].values()); print({k:c for k,c in Counter(v).items() if c>1})"` | **serrano** (研究层) | 2-3 天 | CODE_MAP 值唯一，269 位历史人物对应 269 个唯一代码，无重复实体 |
| **P0-4** | 数据层 | **3 处现代词汇渗入历史案例** — 邵雍北宋出现"AI"、王选案例"软件"、任正非"数据库" | 历史实确性破产，教学/展示误导 | 检查 H-SY-93、H-WX-147、H-RZF-39 的 case 字段 | **serrano** | 1 天 | 所历史人物 case 字段 0 现代技术/商业术语（OKR、AI、软件、数据库、算法等） |
| **P0-5** | 工程层 | **API 服务完全缺失** — 无 FastAPI/Flask、无路由、无 OpenAPI、无契约测试 | 前后端分离阻断，语义搜索不可用 | 访问 http://localhost:8000/docs → 404 | **albo** (工程层) | 5-7 天 | `docker-compose up -d` 后 `curl http://localhost:8000/figures` 返回 200，`/docs` 有 Swagger UI |
| **P0-6** | 工程层 | **Docker 部署缺失** — 无 Dockerfile、无 docker-compose.yml、无健康检查 | 一键启动不可用，CI/CD 无法集成 | `ls <repo>/Dockerfile` → 不存在 | **albo** | 2-3 天 | `docker-compose up -d` 单命令启动全栈，`docker-compose ps` 全 healthy |
| **P0-7** | 工程层 | **语义搜索无评测基线** — 无向量索引、无 Recall 指标、无延迟 SLA | 搜索质量不可量化，发布无依据 | 无自动化评测脚本 | **albo** | 3-5 天 | `pytest tests/regression/test_search.py` 通过，Top-5 Recall ≥ 0.85，p95 < 200ms |

---

## P1 级阻断项（>3 个则暂停发布）

| 编号 | 类别 | 问题描述 | 影响面 | 责任人 | 预计修复时间 |
|------|------|----------|--------|--------|--------------|
| **P1-1** | 数据层 | **场景标签仅 2 条/289 场景** — 标签体系空洞，无法支持分面检索 | 搜索/推荐/分析功能受限 | **elcano** | 3-4 天 |
| **P1-2** | 工程层 | **Web UI 仅静态 HTML** — 无框架、无状态管理、无 SSR、无 i18n 运行时切换 | 生产级 UI 不可用 | **albo** | 5-7 天 |
| **P1-3** | 工程层 | **无 E2E 测试** — 无 Playwright、无 Lighthouse CI、无视觉回归 | 发布风险不可控 | **albo** | 3-4 天 |
| **P1-4** | 安全 | **无安全扫描** — 无 Bandit、无依赖审计、无渗透测试 | 合规风险 | **albo** | 2-3 天 |
| **P1-5** | 工程层 | **技能目录不同步** — `~/.hermes/skills/.../scripts/` 仍为 145 场景旧版 | 下游工具链失效 | **espinosa** (本轮复核后同步) | 0.5 天 |

---

## P2 级改进项（建议，不阻断发布）

| 编号 | 类别 | 问题描述 | 建议 |
|------|------|----------|------|
| **P2-1** | 数据层 | 为 code_maps.json 添加 JSON Schema 验证，防未来重复 | CI 中加入 `jsonschema` 校验 |
| **P2-2** | 数据层 | 增加自动化脚本：定期检查 EN 中文残留、era 完整性、重复人物 | `tools/qa_guard.py` 定时跑 |
| **P2-3** | 文档层 | 生成场景索引文档 `docs/reference/scenarios_index.md` | 用 `python3 tools/thinking_mode_selector.py -e md` |
| **P2-4** | 文档层 | 为 269 位历史人物生成统一格式"思维画像"卡片 | 可选，教学/展示用 |
| **P2-5** | 工程层 | 增加 Grafana/Prometheus 监控栈 | 观测性建设 |

---

## 修复优先级矩阵

```
优先级    │ 并行度    │ 预计总工期 │ 关键路径
──────────┼───────────┼────────────┼────────────────────────────────────
P0 (7项)  │ 全并行    │ 5-7 天     │ P0-1/2/3 (数据层) 与 P0-5/6/7 (工程层) 可并行
P1 (5项)  │ 串行/并行 │ 7-10 天    │ 待 P0 清零后启动
P2 (5项)  │ 闲时      │ 持续       │ 长期技术债偿还
```

---

## 签署与流转

**QA 签署**: Gonzalo Gómez de Espinosa — **NO-GO** (P0 项 7 个，P1 项 5 个)  
**下一步**: 
1. 将 P0-1/2/3/4 分派 **serrano/elcano** 修复数据层
2. 将 P0-5/6/7 分派 **albo** 实现工程层
3. P0 全清后由 **espinosa** 复审，通过则触发 **pigafetta** 染色发布

**复审触发条件**: 所有 P0 项验收标准达标 + `python3 tools/test_thinking_mode_selector.py` 全绿 + `docker-compose up -d` 服务健康