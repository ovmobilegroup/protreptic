# Phase21 W8S2B1 素材包端修复·独立 QA 复核报告（espinosa / 卡 t_a407cb32）

**结论：PASS。五面全绿、零 FAIL、零新增缺陷。** 双锚点独立校验器实测：修复前锚点 32/32（两类缺陷必现）、修复后锚点 33/33、追加 F2 全扫 36/36、追加 push 面 35/35（全部 rc=0）。

被审对象：卡 t_2ce1e334「素材包端修复（elcano）」交付链——
- 提交 A `68fd5569`（4 件产物：工具 + 报告 json/md + 落地报告；回执初版）
- 提交 B `5705b030`（回执补记 1 件）
- 父锚点 `1c00ca8a`（修复前态，本卡复核基线）

复核时点环境：ws HEAD = `7714c9d1`（W7 链，他卡在途）；pb HEAD = `c167cb5`；origin/main = `c167cb53`（远端实况）。本报告全部断言钉定于 `1c00ca8a / 68fd5569 / 5705b030 / 9afe48e / 5b693bb`，不吸收窗口外在途状态。

---

## 0 方法（不采信自述）

1. **独立第二实现**：重写匹配管线（分段 → 弱锚 → 分档 → 近似兜底），参数化为 2×2 组合（matcher{F1 前, F1 后} × 归一集{F2 前, F2 后}），不 import 被审工具、不复制其函数；全部输入（154 行引文、34 件缓存全文、书架/索引）经 `git show <ref>:<path>` 从提交对象读取，全程不写仓内文件。
2. **双锚点**：`--at 1c00ca8a` 必须复现两类缺陷（F1 早退短路 / F2 归一集缺字）——实测必现；默认锚点（HEAD）断言修复后全 PASS。
3. **交付物**：`tools/verify_batch1_sourcing_fix_qa_espinosa.py`（本卡新增，只读；sha256 `2508a3a8c6b3718c…`）+ 本报告 + 证据 JSON `docs/research/phase21w8_stage2_batch1_sourcing_fix_qa_evidence_espinosa.json`。
4. **运行矩阵实测**（本卡证据）：

| 运行 | 命令 | 结果 |
|---|---|---|
| 修复前 | `--at 1c00ca8a` | 32/32 PASS，rc=0 |
| 修复后 | 默认 | 33/33 PASS，rc=0 |
| 修复后+扫描 | `--scan` | 36/36 PASS，rc=0 |
| 修复后+push面 | `--with-remote` | 35/35 PASS，rc=0 |

---

## 1 核验矩阵（卡面五面 → 独立复核结果）

| # | 面 | 靶（被审声明） | 独立手段 | 结果 |
|---|---|---|---|---|
| ① | 缺陷面 F1 | 「首段命中即提前返回」造成核中虚高 | 预锚点段级探针（3 行尾段必失配） | 必现 ✓ |
| ② | 缺陷面 F2 | 归一集缺 U+FE30/U+3000 | 预锚点逐字探针（2 行）+ 40 候选全扫 | 必现 ✓ |
| ③ | 结论面 | 6 行变更与 QA 预测/作者归因相符 | v2 四行终档直读 + 见证核验 + 对账 | 相符 ✓ |
| ④ | 计数面 | 154 行全量、无新增假阳性 | 全量重算 0 差 + 4 组合 + 负对照 16 + 全扫 | 相符 ✓ |
| ⑤ | 回页面 | 零主库写、sha 前后一致 | 窗口 diff + sha 重算 + 侧效复原复核 | 相符 ✓ |
| ⑥ | 提交面 | ws/pb byte-exact、push、parity | 双仓 blob 对照 + ls-remote/ancestry + parity 实测 | 相符 ✓ |

---

## 2 缺陷面（一）：F1「早退短路」（预锚点必现）

修复前 matcher 语义：分段列表**命中首段即提前返回**（`punct` 档），尾段从此不被检查——节引/改写文本可被误判为核中。预锚点三条「核中」行的段级实况（我方探针重算；norm=归一后查，light=去空白直查）：

| row | 书 | 段结构 | 首段 | 尾段 | v1 判 | v2 判 |
|---|---|---|---|---|---|---|
| M-GX-007 | 庄子注 | 2 段（9+16 字） | norm@31258 命中 | 16 字 light/norm **双失配** | variant | null |
| M-WB-010 | 老子注 | 主段 2 段全失配；去冠名 alt（13+12 字） | alt 首段 norm@5435 命中 | 尾 12 字 **双失配** | variant | null |
| M-ZDY-005 | 太极图说 | 2 段（16+28 字） | norm@164 命中 | 28 字 light/norm **双失配** | variant | null |

证据要点：三行尾段均为真实失配——底本与引文在尾段互不连续（GX-007 底本作「推之不去留之不停」等改写；WB-010 尾句「言之者失其常，名之者離其真」全句未见；ZDY-005 尾段出自《通书》本注未引）。修复后（全段命中制）三行正确落 `null`，且 `near_partial=true` / `longest_ratio=0.400 / 0.448 / 0.364` 如实登记——与作者回执数值逐位一致（我方独立复算）。

## 3 缺陷面（二）：F2 归一集缺字（预锚点必现）

字级定位（我方探针，norm→原文字窗逐字符映射）：

| row | 书 | 段长 | 旧归一集 | 新归一集 | 命中字 | v1 判 | v2 判 |
|---|---|---|---|---|---|---|---|
| M-ZXC-008 | 文史通义 | 52 字 | find=-1（失配） | **@60911 命中** | U+FE30「︰」×2（引文「：」U+FF1A 对读） | null | variant |
| M-LJY-002 | 陆九渊集 | 42 字 | find=-1（失配） | **@221825 命中** | U+3000「　」×2（「\n\n　」组合段） | null | variant |

40 候选独立全扫（「PSZ∩非CJK∩非ASCII标点」全字符集，实得 40 枚 == 回执清单）：**仅 U+FE30 → M-ZXC-008、U+3000 → M-LJY-002 两枚各翻转 1 行**；其余 38 枚零翻转；将 40 枚逐一追加进新归一集复跑，**后续翻转 0**（补集充要性成立）。
旁证：作者校验器（t_2ce1e334 交付）重跑与存档 `verify.log` **byte-identical**（16/16 PASS，rc=0）——其工具链自身可复现。

## 4 结论面（终档 + 对账）

v2 终档直读 + 见证复核：

| row | 终档 | 见证 | 复核结论 |
|---|---|---|---|
| M-GX-007 | null | — | 段级失配成立，防改写 glitch 生效 ✓ |
| M-WB-010 | null | — | 同上（含去冠名 alt）✓ |
| M-ZDY-005 | null | — | 同上 ✓ |
| M-ZXC-008 | variant(zh-hant, raw) | `1c09d5d360d70a3d.txt` | 见证文件存在、窗内逐字复核 ✓ |
| M-LJY-002 | variant(zh-hant, raw) | `3249682cf8e37e12.txt` | 直击命中；v1 `sent-composite` 路径升级 ✓ |
| M-DZS-006 | quote(punct, raw) | `e9103adb8b3eea3f.txt` | 仅见证窗刷新（U+3000 归一后窗位移，判档不变）✓ |

**对账（三层）**：
1. 与上游 QA（t_e727be57 §3-F1/F2）预测**逐项相符**：F1 → 3 行退出核中；F2 → null−1/variant+1，且另 1 行路径升级（sent-composite→zh-hant）。
2. 与作者回执 `changed_rows`（6 行字段数 10/10/10/10/6/1）**逐字段相符**（字段名单全等，含 note/elsewhere/比率类）。
3. 计数分解（我方独立重算 == 回执 `per_fix_decomposition`）：v1 `55/22/3/74` → F1 单独 `58/19/3/74` → F2 单独 `54/23/3/74` → 终 `57/20/3/74`（null/variant/quote/cross-lang）；`core_stats` 25→23（见证 21→20 + 节引 4→3）复核一致。

---

## 5 计数面（全量、无新增假阳性）

- **154 行逐字段重算 0 差**：verdict / 档 / 路径 / sent_hits / 比率 / 近似 / 他处 / 见证(file+snippet) 全字段；note 派生一致性另证（含 2 行「（去冠名后）」后缀——主段必失配、alt 必命中，逐行验证）。行集独立推导（18 书 / 154 行 / 跨度 159）与产物全等。
- **负对照**：16 条复跑零假阳性（我方独立重推导 == 存储 16 条，扰动字符集/位置全等）。
- **6 行全量归因**（v1↔v2 字段级):GX-007 / WB-010 / ZDY-005（F1）；ZXC-008 / LJY-002（F2）；DZS-006（窗刷新）；其余 **148 行逐字段全等**；`quote` 154/154 字节全等、`quote_hant` 72 行 154/154 无差异（转换层稳定，非行级扰动）。
- **旁证**：v1 快照 pin `6f3f4580…`（生成时工作区态）→ v2 刷新 `736aab3b…`（提交态）；修复窗口输入 sha 前后一致（见 §6）。

## 6 回页面（零主库写）

- `git diff 1c00ca8a..5705b030 -- data/` → **仅回执一件**；两笔提交白名单 5 件 + 1 件，无夹带。
- 输入 pin：`modes_data` / 书架 / 缓存索引 在 1c00ca8a == 5705b030 == 回执与 v2 产物 pin（sha16 `736aab3b…` / `ca46eb42…` / `24464156…`）✓。
- **侧效还原复核**：`docs/research/phase21w8_stage2_batch1_archive_evidence.json` 当前 sha256 == `1c00ca8a` 提交态（`23e2c803…`）✓（父链验收脚本运行默认落盘该件，修复卡已 git 还原、双仓 byte-exact）。
- **父链影响声明复核**：父链独立验收实测 30/32（2 FAIL = §5.1 表 4 件 sha16 失配 + §4.5 指纹 1 枚）——失配 4 件 == 本卡修复面，属预期；刷新路由 t_e01c8aa0（本卡不越界）✓。

## 7 提交面（双仓 / 推送 / parity）

- **byte-exact**：`68fd5569 → 9afe48e`（工具/报告 4 件）；`5705b030 → 5b693bb`（回执补记）；逐件 sha16 对照一致（`450d0e75 / 17265dd8 / 72b1927c / 74e443cf / 1a62e383`）。
- **push 面**：ls-remote 实测 origin/main = `c167cb53`，修复链两笔（`9afe48e`、`5b693bb`）均为其祖先 ✓；回执留痕「push `7e76967..9afe48e`、ls_remote == LOCAL」与实测一致。
- **parity（复核时点）**：both 4576 / identical 4575 / only-side 0 / content-diff 1（`docs/architecture/static_data_manifest.json`，ws `32c3a490…` vs pb `abf78622…`，**他卡在途**，非本卡面）；本卡面 6 件 0 差异。规模自修复时点（2588/2587）变动系 W7 清档批次纳入 tracked 面所致，与本卡无关。
- **本卡零滞后**：本卡提交（A/B 两笔）随镜像 P1/P2 同批 push，回执见 §9。

## 8 分级发现与登记建议

- **无 FAIL、无功能回归、无新增缺陷。**
- N1（口径刷新，非缺陷）：v1 快照 pin `6f3f4580` → v2 `736aab3b`；根因=生成时工作区态含在途（W5 数据侧 `3238a262` 之前）；修复卡已显式刷新并留痕，复核一致。
- N2（已落实，供对账）：落地报告 §2.1 F4 处置——「21/25 见证 + 4 条节引型显式登记 + 校订注记（口径来源）」与上游 QA F4 要求逐项对应，实测文本核验通过。
- N3（路由）：父链档案 §5.1 登记/§4.5 指纹刷新由 t_e01c8aa0 承接；本卡已把「失配 4 件 == 修复面」账目钉实（sha16 前后对照表见回执），供其直接引用。
- N4（噪声隔离）：复核窗口内 W7/W11 链在途（含 W7 清档、W11 CI 修复），本报告一律钉锚，不吸收其状态。

## 9 提交回执（本卡 A/B 两笔 + 镜像 P1/P2 + push）

<!--RECEIPT-B-->

---

## 附录 A：复现命令

```
python3 tools/verify_batch1_sourcing_fix_qa_espinosa.py                    # 修复后锚点：33/33
python3 tools/verify_batch1_sourcing_fix_qa_espinosa.py --at 1c00ca8a     # 修复前锚点：32/32（缺陷必现）
python3 tools/verify_batch1_sourcing_fix_qa_espinosa.py --scan --json     # +F2 全扫：36/36
python3 tools/verify_batch1_sourcing_fix_qa_espinosa.py --with-remote     # +push 面：35/35（需网络）
python3 tools/check_repo_parity.py                                        # 双仓 parity
```

（校验器只读：全部对象经 `git show` 读取；不加写入类副作用。`--with-remote` 仅追加 fetch/merge-base 元数据操作。）

## 附录 B：证据文件

- `docs/research/phase21w8_stage2_batch1_sourcing_fix_qa_evidence_espinosa.json` —— 结构化证据（锚点/运行矩阵/缺陷探针/重算/负对照/6 行归因/零写/侧效/提交面/回执）。
