# Phase 20 文档归档报告 —— 王阳明 (H-WYM-001)

- 卡片: `t_f7d8763d`（pigafetta，文档归档）
- 上游: `t_cdbbb2d9`（QA 验收，commit `96759545`，独立深检 165/165 PASS）/ 数据落地 `5956dd8b`（elcano，`t_6077a578`）/ 上游研究 `t_18e6db0b`（研究稿派生码 H-WAN-001 / M-WAN-001~010）
- 归档时点: 2026-09-22，`modes_data.json` total=2948（王阳明入库时为 2918；其后并行批次 戴震 +10、章学诚 +10 推进至 2948）；`scenarios_zh`/`scenarios_en` 各 2173
- 产出: `docs/figures/H-WYM-001.md`（46,173 B / 32,853 字符 / 523 行 → 51,958 B / 37,214 字符 / 543 行）、`build_wangyangming_archive.py`（生成器，幂等）、`verify_wym_archive.py`（文档归档层独立验收，27 项）、本报告
- 结论: 档案层 **27/27 PASS**；反向验证（6 处注入缺陷）复现 **8 FAIL**；数据层回归 QA **165/165**、独立复核 **84/84**；入库脚本 58/60（2 项 FAIL 均为并行批次/外部所致，详见 §五）

---

## 一、归档前状态与缺口诊断

王阳明入库卡 `5956dd8b` 已把档案重建为完整档（46,173 B），QA 卡 `96759545` 又对档案热修 13 行（与库内三镜像同步）。本卡复跑 **180 项逐字断言**（10 模式 × 16 字段 = 160 项 + 双语场景 10 对 = 20 项）确认与 `modes_data.json`、`scenarios_zh/en` **零差异**，故定位为**体例收口 + QA 记录补齐**，仍查出 4 项缺口：

| # | 缺口 | 证据（归档前） |
|---|------|---------------|
| 1 | 头部无批次登记链（库内模式 / 上游研究稿 / 数据落地 / QA 验收），仍是入库期的单行「上游研究 … \| 落地入库 …」，与同批最新体例不一致 | 归档前 md 第 4 行 |
| 2 | 「五、跨引用」6 条仅有中文；`code_maps.json` 的 `description_en` 6 条齐备却未落档 | 归档前 md 第 495–501 行 |
| 3 | 「六、现代价值」10 条仅有中文；图档 `modern_value_en` 10 条未落档 | 归档前 md 第 503–514 行 |
| 4 | 「七、入库说明与 QA 验收记录」仍是入库期文本：无 QA 提交号、无三份脚本口径、无 56 条标签清单、无三镜像哈希（仅「SHA256 一致」字样） | 归档前 md 第 515–523 行 |

## 二、修复内容

1. 新增生成器 `build_wangyangming_archive.py`：以库内数据为唯一真值源，**内置「既有正文零改写」逐字断言**（180 项，任一漂移即中止；「五/六」章改前中文行另做逐字比对，旧格式/新格式双认，保证重复运行可幂等），只做四类增量：
   - 头部登记链（`M-WYM-001 ~ M-WYM-010` / 研究稿路径与稿内码 / 数据落地 `5956dd8b` / QA 验收 `96759545`，附 `6e429676` 热修入库注记）；
   - 「五、跨引用」补 6 条英文对照（`code_maps.json` `description_en` 逐字）；
   - 「六、现代价值」补 10 条英文对照（图档 `modern_value_en` 逐字）；
   - 「七」升级为 QA 验收记录：QA 提交号与 5 类 14 处热修明细、三份脚本口径（165 / 84 / 60）、三镜像完整 64 位 SHA256、56 条标签清单、归档时点库总量与场景总量、本卡归档动作说明。
2. 新增文档归档层独立验收脚本 `verify_wym_archive.py`（27 项断言，支持传入任意 md 路径做反向验证；含生成器幂等判据）。

## 三、验收证据（全部实跑）

| 脚本 | 结果 |
|---|---|
| `verify_wym_archive.py`（本卡，27 项） | **27/27 PASS** |
| `verify_wym_archive.py` 反向验证（副本植入 6 处缺陷） | 19 PASS / **8 FAIL**：1.2 头部登记链、2.3 字段逐字（160 项）、4.1 跨引用 5/6、5.2 现代价值 EN、6.1 EN 中文残留、7.2 镜像哈希、9.1/9.2 库总量 —— ✓ 判据非空转 |
| `verify_wym_qa_espinosa.py`（QA 复核，165 项） | **165/165 PASS** |
| `verify_wangyangming_indep.py`（独立复核，84 项，barbosa） | **84/84 PASS** |
| `verify_wangyangming_phase20.py`（入库，60 项，elcano） | 58 PASS / **2 FAIL**：第 2 项硬编码 `total==2918`（现库 2948，并行批次增长）；第 53 项 `M-WAN-001` 全局唯一（H-WFZ-001 同口径冲突）——二者均为外部所致，见 §五；档案层判据 45–50（md > 20KB / 10 模式覆盖 / 定义中英逐字 / 双语场景索引 / 无省略号 / 人物与编码标识）**全 PASS** |
| 生成器幂等 | 连跑两次，第二次 51,958 B 字节不变 |

- 三镜像 SHA256（实算）: `adbc19166eab9ac53ce1b04552faaf33b3506b082296e65d671e29d1afe28ab9`，19,721 B，`data/figures` = `data/individuals` = `docs/figures`，与 QA 报告 §二.4 记载一致；本卡把该完整哈希与体积写入档案「七」，并新增 7.2 判据（档案所载哈希 == 实算哈希）。
- 反向验证明细（注入 → 命中）：头部 `5956dd8b`→`deadbeef` → 1.2；删除 H-SMQ-001 英文对照 → 4.1；改现代价值 EN 一条 → 5.2；`定义(EN)`（M-WYM-008）注入中文 → 2.3 + 6.1；三镜像 SHA256 改 `0`×64 → 7.2；`total=2948`→`2900` → 9.1 + 9.2。

## 四、与同批体例对齐

| 项 | H-LJY-001（上一归档卡） | H-ZXC-001（最新入库） | **H-WYM-001（本卡后）** |
|----|--------------------------|------------------------|--------------------------|
| 档案体积 | 52,292 B | 46,120 B | **51,958 B**（归档前 46,173 B） |
| 头部登记链 | 库内模式 + 研究稿 + 落地 + QA ✓ | 库内模式 + 研究稿 + 落地 + 独立验收 ✓ | **库内模式 + 研究稿 + 落地 + QA（含热修入库注记）✓**（本卡补） |
| 跨引用中英 | EN 引文 ✓（3 条） | — | **EN 引文 ✓**（6 条，本卡补） |
| 现代价值 | 中英对照 ✓（5 条） | — | **中英对照 ✓**（10 条，本卡补） |
| 双语场景索引 | ✓ 10 对 | ✓ 10 对 | ✓ 10 对（逐字） |
| 三镜像 | 齐全（含哈希） | 齐全 | 齐全（含完整哈希 + 体积） |
| 模式字段 | 16 字段（160 项逐字 PASS） | 16 字段 | 16 字段（160 项逐字 PASS，180 项总断言） |
| QA 记录 | QA 提交号 + 修复明细 | 独立验收结果 | QA 提交号 + 5 类 14 处明细 + 三份脚本口径 |

## 五、非范围遗留（供编排侧处置）

1. **`verify_wangyangming_phase20.py` 第 53 项口径冲突（建议裁决）**：该判据要求「`research_mode_id == M-WAN-001` 全局唯一」。git 历史实证：`5956dd8b`（WYM 入库）时计数 **1**（仅 `M-WYM-001`）→ PASS；`04a77491`（WFZ 入库）后计数 **2**（`M-WYM-001` 与 `M-WFZ-001` 均溯源为 `M-WAN-001`）→ FAIL；同批 `verify_wangfuzhi_phase20.py` 第 62 项把同判据**限定在 `M-WFZ` 前缀内**（figure 内唯一），故 WFZ 68/68 PASS。根因：两份上游研究稿（王阳明 `t_18e6db0b`、王夫之）派生的「M-WAN-*」研究稿码天然重名，而两卡验收脚本口径不一致。数据本身 figure 内 1:1 映射正确（图档 `phase20.mode_id_mapping` 齐备），**非 WYM 数据缺陷**；建议编排者裁决统一口径（figure 内唯一）或为 research_mode_id 引入 figure 去重（数据层改动，本卡未动数据层）。
2. `verify_wangyangming_phase20.py` 第 2 项硬编码 `total==2918`：与 LJY 归档报告 §五.1 同类问题（上游脚本对库增长的脆弱性），建议改「≥ 入库时点」区间断言。
3. **全库 `_en` 字段 CJK 残留（系统性）**：LJY QA 报告统计 510 处 / 115 figure；H-WYM-001 自身 1 处（遗留旧码 M383）已由 QA 卡 `96759545` 收口，本卡 6.1 判据复核 PASS；其余仍建议编排者派专项卡统一清理。
4. `H-ZHX-001`（朱熹）、`H-KZ-001`（孔子）未在 `code_maps.figures` 注册（图档文件存在，本图 xref 目标可解析，不构成悬空）——属基线状态，非本卡范围。
5. `representative_cases_zh` 部分对白为情境化改写（QA 报告观察项，未改动）。
6. hotspot: `data/modes_data.json`（多卡共享大文件）与 `docs/figures/H-WYM-001.md`；本卡**只定点提交自己的 4 个路径**（`git commit --only`：档案 md + 生成器 + 验收脚本 + 本报告），未夹带并行批次改动。
