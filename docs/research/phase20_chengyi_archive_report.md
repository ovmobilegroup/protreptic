# Phase 20 文档归档报告 —— 程颐 (H-CHI-001)

- 卡片: t_c7ad2319（pigafetta，文档归档）
- 上游: t_66fa11a9（QA 验收，commit `48ff3736`）/ 数据落地 `a497c57a`（barbosa, t_c64453b8）
- 归档时点: 2026-09-22，库总量 `modes_data.json` total=2918（含并行批次王阳明 H-WYM-001 +10）
- 产出: `docs/figures/H-CHI-001.md`（4,936 B → 42,388 B）、`docs/figures/H-CHI-001.json`（第三镜像，新建）、`build_chengyi_archive.py`、`qa_chengyi_archive.py`、本报告

## 一、诊断：入库期骨架档的四处缺口

| # | 缺口 | 证据 |
|---|------|------|
| 1 | 档案仍是入库期骨架件，仅 4,936 B（同批 H-CHE-001 为 29,797 B，H-LJY-001 为 48,239 B） | `ls -l docs/figures/H-CHI-001.md` 归档前 |
| 2 | 模式小节只有 11 个字段中的 5 个（中文名/定义/类别/来源），缺 英文名规范位、领域(中英)、原文依据英译、操作步骤(中英)、代表性案例(中英)、当代应用(中英)、关联模式 | 归档前 md 第 29–157 行 |
| 3 | 定义行取自**被改写前**的库内精简文本并以「...」截断，与 `modes_data.json` 全文不一致 | 如 M-CHI-001 定义止于「禀受不同故有善恶之分。...」，库内全文续至「……是宋明人性论的基石。」 |
| 4 | 缺「四、双语场景索引」「五、跨引用」及第三镜像 `docs/figures/H-CHI-001.json`（程颢 H-CHE-001 三镜像齐全） | 归档前 md 无四/五章；`docs/figures/` 下无 H-CHI-001.json |

根因：Phase 20 首轮入库时档案按「骨架 + 中文摘要」体例生成，其后 `a497c57a` 把 M-CHI-001~010 正文恢复为源档全文，但**归档文档未随之重建**，因此形成「库内全文 / 文档截断」的体例漂移。这与同批 H-CHE-001、H-LJY-001、H-WYM-001、H-ZDY-001 的「完整档案」体例不一致。

## 二、修复

1. 新增生成器 `build_chengyi_archive.py`：以库内数据为唯一来源重建文档，**「一、历史定位」「二、核心思想」零改写**（含安全网断言：既有正文逐行必须保留），仅：
   - 头部补登记批次链（库内模式 10 码 / 上游研究稿 / 数据落地 / QA 提交）；
   - 二章纯增量补 `**出处**`、`**历史影响**`（取自图档 `famous_quote_source` / `influence`）；
   - 重建「三、十大思维模式」为 16 字段全字段体例；
   - 新增「四、双语场景索引」（C-CHI-001~010 / C-CHI-001E~010E 逐字）；
   - 五章跨引用、六章入库说明与 QA 验收记录（含库总量、标签、三镜像哈希口径）。
2. 新建第三镜像 `docs/figures/H-CHI-001.json`，与 `data/figures/H-CHI-001.json`、`data/individuals/H-CHI-001.json` 字节一致。
3. 新增归档层独立验收脚本 `qa_chengyi_archive.py`（33 项断言），并在其中内置生成器幂等断言。

## 三、验收证据（全部实跑）

- `qa_chengyi_archive.py` → **33/33 PASS**（文档层：16 字段 × 10 模式逐字一致、场景 10 对逐字、跨引用 4 条中英逐字、三镜像 SHA256 一致、无截断残留、生成器幂等）
- `verify_chengyi_phase20.py` → **29/29 PASS**（入库口径回归）
- `qa_chengyi_deep.py` → **81/81 PASS**（上游深检，未回归）
- `verify_chengyi_indep.py` → **401/401 PASS**（独立复核，未回归）
- 三镜像 SHA256: `bb3a33a4148ba90d5de5315a19e54c5e41531539cce1ebcdfc585220feeef347`（data/figures = data/individuals = docs/figures）

## 四、与同批体例对齐

| 项 | H-CHE-001（程颢） | H-LJY/H-WYM/H-ZDY | H-CHI-001（本卡重建后） |
|----|------------------|-------------------|------------------------|
| 档案体积 | 29.8 KB | 46–76 KB | 42.4 KB |
| 模式字段 | 英文名/类别/层级/优先级/领域/出处/定义(中英)/关键概念/原文依据/操作步骤(中英)/代表性案例/当代应用/关联模式 | 三类 EN 块 + 研究稿ID | 同 H-CHE-001 + 代表性案例(EN)/当代应用(EN)；**无**研究稿ID（库内 M-CHI-* 无 `research_mode_id` 字段，故不虚构） |
| 章节 | 一~三 + 跨引用 + 入库说明 | 一~七（含双语场景索引/现代价值） | 一~六（双语场景索引 ✓；**不设「现代价值」**，因本图档无 `modern_value_zh` 字段，与 H-CHE-001 同） |
| 三镜像 | 齐全 | 齐全 | 齐全（本卡补齐第三镜像） |

## 五、非范围遗留事项（供编排侧处置）

1. `data/figure_names.json` 存在数百条非规范/误写键（如 `H-HZX-001_modes` 式），建议单开全库索引净化卡（上游 t_66fa11a9 已提出，未处理）。
2. H-CHI-001 图档 `protreptic_mapping={}`、`mode_evidence=[]`，与 H-CHE-001 同为空——属同批体例差异，若需「现代价值/证据链」章节须先补数据。
3. Phase 20 上游的 git 暂存区污染（弃用版 87e02a5c）在本卡执行前已由并行批次王阳明卡（commit `5956dd8b`）连同工作区一并收口，本卡复核时工作区为干净状态；本卡提交沿用 `git commit --only <paths>` 定点提交，未夹带并行批次改动。
