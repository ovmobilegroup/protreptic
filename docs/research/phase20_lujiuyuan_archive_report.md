# Phase 20 文档归档报告 —— 陆九渊 (H-LJY-001)

- 卡片: `t_6d79091d`（pigafetta，文档归档）
- 上游: `t_42c77bc7`（QA 验收，commit `830e3ccb`，独立深检 98/98 PASS）/ 数据落地 `6830059b`（elcano，`t_3f82e962`）/ 上游研究 `t_ed837a88`（研究稿派生码 H-LUJ-001）
- 归档时点: 2026-09-22，`modes_data.json` total=2928（陆九渊入库时为 2908；其后并行批次 H-WYM-001、H-WFZ-001 各 +10）
- 产出: `docs/figures/H-LJY-001.md`（48,318 B → 52,292 B）、`build_lujiuyuan_archive.py`（生成器）、`verify_ljy_archive.py`（文档归档层独立验收，24 项）、本报告
- 结论: 档案 24/24 PASS，数据层回归 98/98 PASS，无既有正文改写

---

## 一、归档前状态与缺口诊断

与同批 H-CHI-001（入库期骨架档 4.9 KB 需整档重建）不同：陆九渊的入库卡 `6830059b` 已把档案从 Phase 21 骨架件重建为**完整档**——48,318 B（= 33,831 字符）/ 510 行，含 16 字段全字段体例、「四、双语场景索引」、「六、现代价值」。本卡复跑 160 项逐字断言（10 模式 × 16 字段）确认与 `modes_data.json` **零差异**，故本卡定位为**体例收口 + QA 记录补齐**，仍查出 4 项缺口：

| # | 缺口 | 证据（归档前） |
|---|------|---------------|
| 1 | 头部无批次登记链（库内模式 / 上游研究稿 / 数据落地 / QA 验收），仍是入库期的单行「上游研究 … \| 落地 …」，与同批最新体例 H-CHI-001 不一致 | 归档前 md 第 3 行 |
| 2 | 「五、跨引用」3 条仅有中文；`code_maps.json` 的 `description_en` 齐备却未落档（H-CHI-001 同章已含 EN 引文） | 归档前 md 第 492–495 行 |
| 3 | 「六、现代价值」5 条仅有中文；图档 `modern_value_en` 5 条未落档 | 归档前 md 第 497–502 行 |
| 4 | 「七、入库说明与 QA 验收记录」仍是入库期文本：镜像口径误写「图档**双**镜像」而同句并列三处路径；无 QA 提交号、无验收脚本口径、无标签清单、无三镜像哈希 | 归档前 md 第 504–509 行 |

**口径提示**：QA 报告 §二.6 记「档案 md 33831 B」，git 实测同为 48,318 B —— 33,831 是**字符数**（`len(str)`），48,318 是**字节数**。同批报告建议统一标注单位。

## 二、修复内容

1. 新增生成器 `build_lujiuyuan_archive.py`：以库内数据为唯一真值源，**内置「既有正文零改写」逐字断言**（10 模式 × 16 字段 + 双语场景 10 对，任一漂移即中止），只做三类增量：
   - 头部登记链（`M-LJY-001 ~ M-LJY-010` / 研究稿路径与派生码 / 数据落地 `6830059b` / QA 验收 `830e3ccb`）；
   - 「五、跨引用」补 3 条英文对照（`code_maps.json` `description_en` 逐字）；
   - 「六、现代价值」补 5 条英文对照（图档 `modern_value_en` 逐字）；
   - 「七、入库说明与 QA 验收记录」升级：补 QA 提交号与 5 处 `_en` 修复明细、三份脚本口径、三镜像哈希（完整 64 位 SHA256）、36 条标签清单、归档时点库总量；并更正「图档双镜像」→「图档三镜像」。
2. 新增文档归档层独立验收脚本 `verify_ljy_archive.py`（24 项断言，支持传入任意 md 路径以做反向验证）。

## 三、验收证据（全部实跑）

| 脚本 | 结果 |
|---|---|
| `verify_ljy_archive.py`（本卡，24 项） | **24/24 PASS** |
| `verify_ljy_archive.py` 反向验证（对同一档案植入 5 处缺陷） | 18 PASS / **6 FAIL**（2.3 字段逐字、3.1 场景逐字、4.1 跨引用 2/3、5.2 现代价值 EN、6.1 EN 中文残留、7.2 镜像哈希）✓ 判据非空转 |
| `verify_ljy_qa_espinosa.py`（QA 复核，98 项） | **98/98 PASS** |
| `verify_lujiuyuan_phase20.py`（入库，52 项） | 51 PASS / 1 FAIL —— 第 2 项断言硬编码 `total==2908`，现库 2928（并行批次增长）所致；档案层 39–44 项（md > 20KB / 10 模式覆盖 / 定义中英逐字 / 双语场景索引 / 无省略号 / 无悬空引用）**全 PASS** |
| 生成器幂等 | 连跑两次，第二次 52,292 B 字节不变 |

- **回归拦截记录（诚实披露）**：初稿把三镜像哈希记作缩写 `8a61a691…ede8`，触发上游第 43 项与 QA 6.3「档案 md 无省略号」判据（QA 98 → 97 PASS）。改为完整 64 位 SHA256 后，QA 回归 **98/98**、上游档案层判据全 PASS。本卡因此新增判据 6.2（全文无 `...`/`…`/TODO/占位符）并把 7.2 收紧为「档案所载哈希 == 实算哈希」。
- 三镜像 SHA256（实算）: `8a61a6912caa5c8273b3eff13504ac76f8cc0bd1b9b31077ff42cdab0a8cede8`，15,226 B，`data/figures` = `data/individuals` = `docs/figures`。

## 四、与同批体例对齐

| 项 | H-CHE-001（程颢） | H-CHI-001（程颐） | H-ZDY-001（周敦颐） | **H-LJY-001（本卡后）** |
|----|------------------|------------------|--------------------|------------------------|
| 档案体积 | 29,797 B | 42,388 B | 75,835 B | **52,292 B**（归档前 48,318 B） |
| 头部登记链 | 库内模式 + 研究稿 + 落地 | 库内模式 + 研究稿 + 落地 + QA | 上游 + 落地 | **库内模式 + 研究稿 + 落地 + QA** ✓ |
| 跨引用中英 | EN 引文 ✓ | EN 引文 ✓ | 仅中文 | **EN 引文 ✓**（本卡补） |
| 现代价值 | 无该章（无字段） | 无该章（无字段） | 仅中文 | **中英对照 ✓**（本卡补 EN） |
| 双语场景索引 | 无 | ✓ 10 对 | ✓ 10 对 | ✓ 10 对（逐字） |
| 三镜像 | 齐全 | 齐全（第三镜像本批补） | 齐全 | 齐全 |
| 模式字段 | 12–14 字段 | 16 字段 | 16 字段 | 16 字段（160 项逐字 PASS） |

## 五、非范围遗留（供编排侧处置）

1. **上游脚本可复现性**：`verify_lujiuyuan_phase20.py` 硬编码 `R="/opt/data/workspace/Protreptic"` 与 `total==2908`，导致冻结快照无法独立复跑、库增长后首页即 FAIL（本卡实测：即使 `git archive 830e3ccb` 解包，脚本仍读主工作区）。建议后续脚本改相对路径 + 「≥ 入库时点」区间断言。
2. 报告单位口径混用（QA 报告 §二.6 的 33,831 为字符数而非字节数），建议后续统一标注。
3. **全库 `_en` 字段中文残留 510 处 / 115 figure**（QA 卡 `t_42c77bc7` 已提出，含 H-WYM-001 旧码 `M383` 1 处）。陆九渊自身 5 处已由该 QA 卡清零，本卡复核确认无残留；建议编排者统一派一轮全库清理专项卡。
4. `H-ZHX-001`（朱熹）尚未在 `code_maps.figures` 注册（属朱熹卡范围；本图 xref 目标可解析，不构成悬空）。
5. hotspot: `docs/figures/H-LJY-001.md`、`data/modes_data.json` 为多卡共享面；本卡**只定点提交自己的 4 个路径**（`git commit --only`），未夹带并行批次改动的文件。
