# Phase 20 文档归档报告 —— 王夫之 (H-WFZ-001)

- 卡片: `t_916357e9`（pigafetta，文档归档）
- 上游: `t_ef2f2b30`（QA 验收，commit `99e0378c`，独立深检 76/76 PASS）/ 数据落地 `04a77491`（elcano，`t_eb872f66`，20260922_145939）/ 上游研究 `t_25de07b9`（研究稿派生码 H-WAN-001 / M-WAN-001~010）
- 归档时点: 2026-09-22 16:40（CST），`modes_data.json` total=2948（王夫之入库 2918→2928；其后并行批次推进至 2948）；`scenarios_zh`/`scenarios_en` 各 2173；`code_maps.figures` 211
- 产出: `docs/figures/H-WFZ-001.md`（53,330 B / 37,070 字符 / 496 行 → **59,163 B / 41,039 字符 / 508 行**）、`build_wangfuzhi_archive.py`（生成器，幂等）、`verify_wfz_archive.py`（文档归档层独立验收，30 项）、本报告
- 结论: 档案层 **30/30 PASS**；反向验证（6 处注入缺陷）复现 **8 FAIL**；数据层回归：QA 复核 **64/64 PASS**、上游入库脚本 **68/68 PASS**；提交仅含本卡 4 个路径（`git commit --only`）

---

## 一、归档前状态与缺口诊断

王夫之入库卡 `04a77491` 已把档案重建为完整档（52.3 KB 体例），QA 卡 `99e0378c` 又对档案热修 1 行（与库内三镜像同步）。本卡复跑 **180 项逐字断言**（10 模式 × 16 字段 = 160 项 + 双语场景 10 对 = 20 项）确认与 `modes_data.json`、`scenarios_zh/en` **零差异**，故定位为**体例收口 + QA 记录补齐**，仍查出 4 项缺口：

| # | 缺口 | 证据（归档前） |
|---|------|---------------|
| 1 | 头部无批次登记链（库内模式 / 上游研究稿 / 数据落地 / QA 验收），仍是入库期的单行「上游研究 … \| 落地入库 …」，与同批最新体例不一致 | 归档前 md 第 4 行 |
| 2 | 「五、跨引用」2 条仅有中文；`code_maps.json` 的 `description_en` 2 条齐备却未落档 | 归档前 md 第 478–479 行 |
| 3 | 「六、现代价值」5 条仅有中文；图档 `modern_value_en` 5 条未落档 | 归档前 md 第 482–486 行 |
| 4 | 「七、入库说明与 QA 验收记录」仍是入库期文本：无 QA 提交号、无脚本口径、无 76 条标签清单、无三镜像完整哈希（仅「SHA256 一致」字样） | 归档前 md 第 489–496 行 |

## 二、修复内容

1. 新增生成器 `build_wangfuzhi_archive.py`：以库内数据为唯一真值源，**内置「既有正文零改写」逐字断言**（180 项，任一漂移即中止；「五/六」章改前中文行另做逐字比对，旧格式/新格式双认，保证重复运行可幂等），只做四类增量/升级：
   - 头部登记链（`M-WFZ-001 ~ M-WFZ-010` / 研究稿路径与稿内码 / 数据落地 `04a77491` / QA 验收 `99e0378c`）；
   - 「五、跨引用」补 2 条英文对照（`code_maps.json` `description_en` 逐字）；
   - 「六、现代价值」补 5 条英文对照（图档 `modern_value_en` 逐字）；
   - 「七」升级为 QA 验收记录：QA 提交号与 4 类 15 处热修明细、三份数据层脚本口径（76 / 64 / 68）、三镜像完整 64 位 SHA256 与体积、76 条标签清单、归档时点库总量与场景总量、QA 遗留观察、本卡归档动作说明。
2. 新增文档归档层独立验收脚本 `verify_wfz_archive.py`（30 项断言，支持传入任意 md 路径做反向验证；含生成器幂等判据与 xref 目标可达性判据）。

## 三、验收证据（全部实跑）

| 脚本 | 结果 |
|---|---|
| `verify_wfz_archive.py`（本卡，30 项） | **30/30 PASS** |
| `verify_wfz_archive.py` 反向验证（副本植入 6 处缺陷） | 22 PASS / **8 FAIL**：1.2 头部登记链、2.3 字段逐字（160 项）、4.1 跨引用 1/2、5.2 现代价值 EN、6.1 EN 中文残留、7.2 镜像哈希、9.1/9.2 库总量 —— ✓ 判据非空转 |
| `verify_wfz_qa_espinosa.py`（QA 复核，状态组 64 项） | **64/64 PASS**（归档后复跑，含 7.4/7.5 档案层判据） |
| `verify_wangfuzhi_phase20.py`（入库，68 项，elcano） | **68/68 PASS**（含第 55–59 项档案 md 判据） |
| 生成器幂等 | 连跑两次，第二次 59,163 B 字节不变 |

- 三镜像 SHA256（实算）: `aa4eedb10546701b2de66c7b24def68e5f182d306a77f59dacf4b7b1cd89160c`，16,889 B，`data/figures` = `data/individuals` = `docs/figures`，与 QA 报告 §二.7 记载一致；本卡把该完整哈希与体积写入档案「七」，并新增 7.2 判据（档案所载哈希 == 实算哈希）。
- 反向验证明细（注入 → 命中）：头部 `04a77491`→`deadbeef` → 1.2；删除 H-GYW-001 英文对照 → 4.1；改现代价值 EN 一条 → 5.2；`定义(EN)`（M-WFZ-008）注入中文 → 2.3 + 6.1；档案所载镜像 SHA256 改 `0`×64 → 7.2；`total=2948`→`2900` → 9.1 + 9.2。
- 回归要点（实跑发现并处置）: 档案「七」若**逐字引用**修复前的错误串（`most + fuller` 的连写形式），会触发 QA 脚本 8.5 的产物级子串判据（该判据的 `blob_all` 含档案 md）——本卡改述为「双重比较级 `most + fuller` 错误 → `the fullest state of qi`」，复跑 QA 脚本回到 **64/64 PASS**。教训：缺陷清零型判据与「如实记录修复」的文档措辞存在耦合，引用错误串时需避免其连写形式。

## 四、与同批体例对齐

| 项 | H-LJY-001（上上归档卡） | H-WYM-001（上一归档卡） | **H-WFZ-001（本卡后）** |
|----|--------------------------|--------------------------|--------------------------|
| 档案体积 | 52,292 B | 51,958 B | **59,163 B**（归档前 53,330 B；标签 76 条为同批最多） |
| 头部登记链 | 库内模式 + 研究稿 + 落地 + QA ✓ | 库内模式 + 研究稿 + 落地 + QA（含热修入库注记）✓ | **库内模式 + 研究稿 + 落地 + QA（热修随本提交入库）✓**（本卡补） |
| 跨引用中英 | EN 引文 ✓（3 条） | EN 引文 ✓（6 条） | **EN 引文 ✓**（2 条，本卡补） |
| 现代价值 | 中英对照 ✓（5 条） | 中英对照 ✓（10 条） | **中英对照 ✓**（5 条，本卡补） |
| 双语场景索引 | ✓ 10 对 | ✓ 10 对 | ✓ 10 对（逐字） |
| 三镜像 | 齐全（含哈希） | 齐全（含完整哈希 + 体积） | 齐全（含完整哈希 + 体积，本卡写入档案） |
| 模式字段 | 16 字段 | 16 字段（160 项逐字 PASS，180 项总断言） | 16 字段（160 项逐字 PASS，180 项总断言） |
| QA 记录 | QA 提交号 + 修复明细 | QA 提交号 + 5 类 14 处明细 + 三份脚本口径 | QA 提交号 + 4 类 15 处明细 + 三份脚本口径 + 遗留观察 |
| 文档层脚本 | `verify_ljy_archive.py`（24 项） | `verify_wym_archive.py`（27 项） | **`verify_wfz_archive.py`（30 项）** |

## 五、非范围遗留（供编排侧处置）

1. `verify_wangfuzhi_phase20.py` 第 62 项将 `research_mode_id` 唯一性**限定在 `M-WFZ` 前缀内**（figure 内唯一），与 `verify_wangyangming_phase20.py` 第 53 项的全局唯一口径不一致——王阳明归档报告 §五.1 已提出裁决请求，属编排侧决策（数据本身 figure 内 1:1 映射正确）。
2. 存量数据观察（QA 报告 §四，本卡零新增、不阻塞）：研究稿 M-WAN-004 引文标点微差（「性者生理也」vs 库内「性者，生理也」）；`C-IKH-008` 1 处 U+FFFD 替换字符；全库 6 条存量悬空 xref（H-DZS-001→GZ-XZ-001 等）；`H-GYW-001`（顾炎武）未在 `code_maps.figures` 注册。
3. 字「而农」的罗马化：库内既有方案为 `E-nong`，学界通行转写为 `Ernong`（QA 报告 §三.2 观察）——是否全局统一留待后续决策，本卡未动数据层。
4. 「缺陷清零」型验收脚本与档案文档措辞的耦合（见 §三回归要点）：后续归档卡在档案中记录修复前错误串时，建议避免其连写形式或让此类判据排除 `docs/figures/*.md`。
5. hotspot: `data/modes_data.json`（23 MB）与 `docs/figures/H-WFZ-001.md`；本卡**只定点提交自己的 4 个路径**（`git commit --only`：档案 md + 生成器 + 验收脚本 + 本报告），未夹带并行批次改动。
